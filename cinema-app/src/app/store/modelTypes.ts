export type Movie = {
    id: number,
    title: string,
    description: string,
    rating?: string,
    duration?: string,
    genre?: string,
    releasedDate: string,
    image?: string,
    trailerLink: string
  };